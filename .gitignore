# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
scratch/
.vercel
.claude/
*.env
*.env.*

# Phase 0: heavy, deterministically regenerable analysis intermediates + gate captures.
# Recreate with `npm run derive-reference`. The shipped assets live in public/reference/.
reference/derived/

# Local visual-review scratch (Playwright MCP writes snapshots/logs here)
.playwright-mcp/

# Local Playwright / motion-gate capture output (review artefacts, regenerable)
output/

# Local brainstorming companion screens/server state
.superpowers/
